ext {
    extName = 'OPSCANS'
    pkgNameSuffix = 'en.opscans'
    extClass = '.OpScans'
    extVersionCode = 1
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib-dataimage"))
}
